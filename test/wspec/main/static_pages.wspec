requirement "The pseudo-static pages should be correctly served" do
  
  therefore("They should be reachable directly") {
    locations = %w{index activites latex securite-vie-privee/index securite-vie-privee/orateurs
                   securite-vie-privee/acces-inscription securite-vie-privee/sponsors events/index
                   mentions-legales contact} 
    locations.each do |loc|
      i_reach Waw.config.web_base + loc
      i_see "<!-- pages/#{loc}.wtpl -->"
    end
  }
  
end
