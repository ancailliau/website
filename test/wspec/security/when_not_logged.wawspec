scenario __FILE__ do 
  go Waw.config.web_base
  
  user = {:mail => "test-user@acmscw.be"}
  assert_service_invoke "User must be logged for updating account", {
    :service => "/webserv/people/update_account",
    :arguments => user, 
    :result  => "validation-ko/user_must_be_logged"
  }

  go '/people/my_chapter'
  assert_i_see Waw.resources.messages.user_must_be_logged
end