requirement "The olympiades activity menu is correctly written" do
  
  therefore("The olympiades index page should not contain lost internal links") {
    i_reach index_page + 'olympiades'
    i_could_reach all_internal_links
  }
  
  #
  # Commented, not because it fails, but because it is time consuming.
  # Webmasters will check this from time to time
  #
  # therefore("Each of its sub pages is well written") {
  #   %w{index equipe demi-finales formation inscription olympiades-internationales questions reglement}.each {|word|
  #     i_reach index_page + "olympiades/#{word}"
  #     i_could_reach all_internal_links
  #   }
  # }
  
  
end
