scenario __FILE__ do 
  go Waw.config.web_base
  
  user = {:mail => "test-user@acmscw.be"}
  assert_service_invoke "validation_ko/user_must_be_logged", "User must be logged for updating account" do
    service "/services/update_account", {}
  end
end